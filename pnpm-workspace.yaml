packages:
  # workers are managed independently; add them here after running pnpm install locally
  - '!**/workers/**'
  - '!**/test/**'
allowBuilds:
  '@parcel/watcher': false
  cypress: false
  esbuild: false
  sharp: false
